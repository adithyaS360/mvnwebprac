---
- name: Deploy to Tomcat
  hosts: web
  become: yes

  tasks:

    - name: Copy WAR file to Tomcat
      copy:
        src: target/Mw.war
        dest: /opt/tomcat/webapps/Mw.war
        remote_src: no
